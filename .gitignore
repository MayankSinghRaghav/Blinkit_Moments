node_modules
.next
.env*.local
*.tsbuildinfo
next-env.d.ts

# large raw scrapes are reproducible via the discovery scripts
data/raw/

# The blank coding sheet is regenerated per run. The human-coded sheet and the
# resulting kappa are NOT ignored: they are the evidence behind the reported
# agreement score, and /discovery reads the report at build time.
data/holdout.csv

# local quota bookkeeping, specific to one machine and one day
data/tag-progress.json
