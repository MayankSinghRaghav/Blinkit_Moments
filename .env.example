# All optional. Missing keys => deterministic matcher + in-memory adoption store.
GEMINI_API_KEY=

# Server-only. Do NOT use the NEXT_PUBLIC_ prefix: Next inlines those at build
# time, so a value added after a build never takes effect until you rebuild.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
